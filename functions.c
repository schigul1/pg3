#include "functions.h"
#include <limits.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>

unsigned int safe_add(unsigned int n1, unsigned int n2) { return 0;}
unsigned int min(unsigned int x, unsigned int y) { return 0;}
unsigned int **read_table(const char *filename, size_t *sz) { return NULL; }
unsigned int dijkstra(unsigned int src, unsigned int dst, unsigned int **table,
                      size_t sz) { return 0; }